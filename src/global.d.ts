type Nullable<T> = T | null;

type Coordinate = number;
type Radius = number;

type BallEntity = {
  x: Coordinate;
  y: Coordinate;
  radius: Radius;
};

type Color = string;
type PixelsAmount = number;
